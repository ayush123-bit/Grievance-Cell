<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complaint Submission Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;

        }

        .submission-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            background-color: #dadada;
            box-shadow: 10 10 5px rgba(0, 0, 0, 0.3);
            opacity: 0;
            animation: fadeIn 1s ease-in-out forwards, bounce 0.5s ease-in-out 1s forwards;
           
        }

        .submission-message {
            position: relative;
            margin-top: 60px;
            height: 20vh;
            border:   #282525d9; /* Border color for the message container */
            text-align: center;
            font-size: 24px;
            color: #474545;
            text-shadow: 2px 2px 4px rgba(11,13,13, 0.5);
            opacity: 0;
            animation: fadeIn 1s ease-in-out 0.5s forwards, typewriter 1s steps(20) forwards;
            margin-bottom: 20px;
            box-shadow: 10 10 5px rgba(184, 178, 178, 0.812); /* Box shadow for the message container */
        }  

        button {
            padding: 10px 20px;
            position: relative;
            margin-top: 190px;
            font-size: 22px;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            background-color: #4b4949;
            color: #fff;
            
            cursor: pointer;
            opacity: 0;
            
           
            animation: fadeIn 1s ease-in-out 1.5s forwards;
            border: 2px solid #1e434d; /* Border color for the button */
            box-shadow: -4px 17px 20px 3px rgba(0, 0, 0, 0.791); /* Box shadow for the button */
        }
        button:hover{
            color: #080808;
            background-color: #eef1dc;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        @keyframes typewriter {
            from {
                width: 0;
            }
            to {
                width: 100%;
            }
        }
        #heading{
            position: relative;
            margin-top: 120px;
            padding: 6px;
            box-shadow: 20px 5px 12px 20px rgb(72, 74, 75);
            
        }
    </style>
</head>
<body>
    <div class="submission-container">
        <div class="submission-message">
            <div id="heading">
                <h1>UPDATED SUCCESSFULLY!</h1>
            </div>
        </div>
        <div>
            <button onclick="window.location.href='/'">BACK TO HOME</button>
        </div>
    </div>
</body>
</html>
