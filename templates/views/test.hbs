<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complaints Dashboard</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: linear-gradient(to right, #0811172b, #2c3e50);
            overflow: hidden;
        }

        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: #50666b;
            padding: 10px 0;
            text-align: center;
            z-index: 1000;
            display: flex;
            justify-content: flex-start;
            align-items: center;
        }

        header h1 {
            color: #fff;
            margin: 0;
            margin-right: auto;
        }

        header a {
            display: inline-block;
            padding: 10px 20px;
            margin-right: 10px;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            transition: background-color 0.3s;
        }

        header a:hover {
            background-color: #aff205;
        }

        #dashboard {
            margin-top: 80px; /* Adjust as needed */
            background-color: #fff;
            border-radius: 10px;
            box-shadow: -20px -14px 18px 6px rgb(2 54 68);
            padding: 23px;
            width: 80%;
            max-width: 632px;
            text-align: center;
            position: relative;
            animation: slideInAndShake 1.5s ease-out;
        }

        .category {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #3498db;
        }

        .teacher-name {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 15px;
            border-radius: 8px;
            transition: box-shadow 0.3s, transform 0.3s;
            animation: fadeInDown 0.5s ease-out;
            background-color: #3498db;
            color: #fff;
        }

        .semester-section-selector {
            margin-bottom: 20px;
        }

        /* Style the dropdowns */
        select {
            width: 100%;
            padding: 10px;
            margin-top: 8px;
            margin-bottom: 16px;
            border: 1px solid #3498db;
            border-radius: 4px;
            box-sizing: border-box;
            background-color: #ecf0f1;
            color: #333;
            transition: background-color 0.3s, color 0.3s;
        }

        /* Style the links as buttons with hover effect */
        .complaint-links {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .complaint-links a {
            display: inline-block;
            padding: 12px 24px;
            margin: 10px 5px;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            transition: transform 0.3s;
            text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.2);
        }

        .complaint-links a.resolved {
            background-color: #4caf50;
        }

        .complaint-links a.unresolved {
            background-color: #e74c3c;
        }

        .complaint-links a.all {
            background-color: #3498db;
        }

        .complaint-links a:hover {
            transform: scale(1.05);
        }

        .background-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://subtlepatterns.com/patterns/axiom.png');
            opacity: 0.1;
            pointer-events: none;
            z-index: -1;
        }

        /* Animation */
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Animation for left to right movement with tilt and shake */
        @keyframes slideInAndShake {
            0% {
                opacity: 0;
                transform: translateX(-100px) rotate(-5deg);
            }
            50% {
                transform: translateX(0) rotate(0);
            }
            100% {
                opacity: 1;
                transform: translateX(0) rotate(0);
            }
        }
        #a1{
            font-size: 30px;
            font-family: Arial, Helvetica, sans-serif;
         
        }
        #a2{
            font-size: 30px;
            font-family: Arial, Helvetica, sans-serif
        }
    </style>
</head>
<body>

    <header>
       <h1>WELCOME TO UNITED</h1>
        <a id="a1" href="#" class="nav-link">Link 1</a>
        <a id="a2" href="#" class="nav-link">Link 2</a>
    </header> 

    <div id="dashboard">
        <div class="background-pattern"></div>
        <div class="category">Hostel Department</div>
        
        <div class="teacher-name">Teacher Name: Ram Singh</div>

        <div class="semester-section-selector">
            <label for="complaintCategory">Faculty: Category</label>
            <select id="complaintCategory">
                <option value="financial">Financial</option>
                <option value="hostel">Hostel</option>
                <option value="registration">Registration</option>
                <option value="admission">Admission</option>
                <option value="scholarship">Scholarship</option>
                <option value="internship">Internship</option>
                <option value="library">Library</option>
            </select>

            <label for="branch">Select Branch:</label>
            <select id="branch">
                <option value="financial">Chemical</option>
                <option value="computer">Computer Science</option>
                <option value="financial">Electrical</option>
                <option value="computer">Civil</option>
            </select>

            <label for="semester">Select Semester:</label>
            <select id="semester">
                <option value="1">Semester 1</option>
                <option value="2">Semester 2</option>
            </select>

            <label for="section">Select Section:</label>
            <select id="section">
                <option value="A">Section A</option>
                <option value="B">Section B</option>
            </select>
        </div>

        <div class="complaint-stats">
            <h3>Complaint Statistics</h3>
            <div class="complaint-links">
                <div class="tooltip">
                    <a href="#resolved" class="resolved">Resolved</a>
                    <span class="tooltiptext"></span>
                </div>
                <div class="tooltip">
                    <a href="#unresolved" class="unresolved">Unresolved</a>
                    <span class="tooltiptext"></span>
                </div>
                <div class="tooltip">
                    <a href="#all" class="all">All</a>
                    <span class="tooltiptext"></span>
                </div>
            </div>
        </div>

        <div class="college-info">
            <h2>College Information</h2>
            <p>Welcome to Our College Dashboard. Here, you can manage and view complaints related to different departments.</p>
            <p>For any assistance, please contact the college administration.</p>
        </div>
    </div>

</body>
</html>
