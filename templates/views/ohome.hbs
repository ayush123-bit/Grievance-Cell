<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>College Complaints Portal</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            overflow: hidden;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            perspective: 1000px;
            perspective-origin: center;
        }

        .outer-container {
            position: relative;
            width: 100vw;
            height: 100vh;
            overflow: hidden;
        }


        @keyframes backgroundScroll {
            0% {
                transform: translateZ(0);
            }
            100% {
                transform: translateZ(-3000px);
            }
        }

        .container {
            background-color: rgb(224 223 223 / 71%);
            padding: 20px;
            border-radius: 10px;
            box-shadow: -20px 15px 20px 15px rgb(137 72 72 / 52%);
            transform-origin: center;
            animation: tiltAnimation 3s infinite alternate-reverse;
            position: relative;
        }

        #photo {
            h
            width: 180vh;
            height: 396px;
            background: url('css/complaint.jpg') center/cover fixed;
            border-radius: 10px;
            overflow: hidden;
            margin-top: 20px;
            
        }

        @keyframes changeImage {
            0%, 20%, 100% {
                background-position: 0 0;
            }
            25%, 45% {
                background-position: 0 100%;
            }
            50%, 70% {
                background-position: 0 200%;
            }
            75%, 95% {
                background-position: 0 300%;
            }
        }

        @keyframes tiltAnimation {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(5deg);
            }
        }

        h1 {
            color: #000000;;
            font-size: 2em;
            margin-bottom: 20px;
            text-shadow:  1px 7px 8px rgba(0, 0, 0, 0.5);
            font-weight: 12px;
            margin-left:367px;
        }

        .options {
            display: flex;
            margin-top: 20px;
        }

        .option {
     margin: 0 220px;
    padding: 24px 85px;
    background-color: #0e5e5cd9;
    color: #fff;
    text-decoration: none;
    border-radius: 4px;
    transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .option:hover {
            background-color: #0056b3;
            transform: scale(1.1) rotate(5deg);
        }
        #ou{
            
            box-shadow: 8px -1px 14px 1px rgb(71, 41, 41), -1em 0 0.4em rgb(21 20 41 / 72%);
    height: 95vh;
    width: 189vh;
 
}
a{
    font-weight: 30px;
    font-size: 35px;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
        
    </style>
</head>
<body>
   <div  id="ou">
        <div class="inner-container"></div>
        <div class="container">
            <h1 class="blinking-text">Welcome to the College Complaints Portal</h1>
            <div class="options">
                <a href="/teacherlogin" class="option">Login</a>
                <a href="/studentlogin" class="option">STUDENT</a>
            </div>
            <div id="photo"></div>
        </div>
    </div>
</body>
</html>
