<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>Login Page</title>
    <style>
        body {
    margin: 0;
    overflow: hidden;
}

.background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    animation: backgroundAnimation 10s infinite linear;
    background-size: cover;
}

@keyframes backgroundAnimation {
    0% {
        background-image: url('css/coll4.jpg');
    }
    50% {
        background-image: url('css/coll.jpg');
    }
    100% {
        background-image: url('css/coll5.jpg');
    }
}

.content-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 100vh;
    overflow: hidden;
    
    
}

.content {
    flex: 2;
    padding: 50px;
    color: #f8fdfdf0;
    font-size: 20px;
    font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
    z-index: 1;
    box-shadow: 10px 0 20px rgba(61, 59, 59, 0.2);
    text-align: center;
    transform: translateX(-100%);
    animation: slideLeft 1s forwards;
}

.login-container {
    flex: 1;
    padding: 30px;
    background: rgba(139, 143, 150, 0.56);
    border-radius: 8px;
    box-shadow: -10px 0 20px rgba(67, 63, 63, 0.2);
    z-index: 1;
    text-align: center;
    transform: translateX(100%);
    animation: slideRight 1s forwards;
}

h1 {
    font-size: 24px;
    color: rgb(94, 222, 222);
    margin-bottom: 20px;
    text-shadow: 2px 2px 4px rgba(36, 28, 28, 0.745);
}

input {
    width: calc(100% - 24px);
    padding: 12px;
    margin-bottom: 15px;
    border: 1px solid #8a8383;
    border-radius: 4px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    transition: box-shadow 0.3s;
}
#h{
    text-shadow: #646262;
    color: rgb(223, 229, 227);
}

input:hover {
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
}

button {
    background-color: #3498db;
    color: #fff;
    padding: 12px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s;
}
.div{
     box-shadow: 10px 10px 5px rgba(129, 108, 208, 0.851);
}

button:hover {
    background-color: #2980b9;
}

@keyframes slideLeft {
    to {
        transform: translateX(0);
    }
}

@keyframes slideRight {
    to {
        transform: translateX(0);
    }
}

    </style>
</head>
<body>
    <div class="background"></div>
    <div class="content-container">
        <div class="content">
        <div class="div">
            <h2 id="h" style="color:white;">WELCOME TO UNITED</h2>
    </div>
            <div class="quotesa" ><h1 id="a">{{quotes}}</h1></div>
       <div class="quotesb"><h1 id="b" style="color: orange;" >~{{name}}</h1></div>
        </div>
        
        <div class="login-container">
            <h1>Login Form</h1>
            <form action="/studentlogin" method="POST">
                <div class="input-container">
                    <p style="color: white;">{{p}}</p>
                    <input type="text" placeholder="Name" name="name" value="{{name1}}" >
                </div>
                <div class="input-container">
                    <input type="email" placeholder="Email" name="email" value="{{email}}">
                </div>
                <div class="input-container">
                    <input type="tel" placeholder="Phone" name="pn" value="{{pn}}">
                </div>
                <div class="input-container">
                    <input type="text" placeholder="Student ID" name="sid" value="{{sid}}">
                </div>
                
                <button type="submit">Login</button>
            </form>
        </div>
    </div>



</body>
</html>
